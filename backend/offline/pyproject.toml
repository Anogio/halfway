[project]
name = "transit-offline"
version = "0.1.0"
description = "Offline GTFS preprocessing and artifact generation"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.13.0",
]
